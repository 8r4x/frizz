---
name: high
description: Effort high. The model is the Agent tool's `model` parameter (omitted, it inherits the worker's).
effort: high
---

Execute the self-contained task in your dispatch prompt.
